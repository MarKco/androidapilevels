<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Compose-only app (night mode). -->
    <style name="Theme.AndroidSDKVersions" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorPrimary">@color/app_primary</item>
        <item name="android:colorBackground">@color/another_row_background</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/status_bar_translucent_dark</item>
    </style>
</resources>